@model IEnumerable<ShopBanChuot.Models.XUATXU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}



<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenXX)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenXX)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaXX }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaXX }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaXX })
        </td>
    </tr>
}

</table>
